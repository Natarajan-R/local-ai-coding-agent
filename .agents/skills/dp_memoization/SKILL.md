---
name: Dynamic Programming Memoization Hint
triggers: book_store, book-store, group allocation, knapsack, subsets, combinations, dynamic programming, memoization
---
When implementing group allocation optimizations, subset scoring, or knapsack variations (e.g., Book Store combinations), always use a dictionary-based memoization cache or a classic dynamic programming grid (`dp = {}`) to prevent recursive execution timeouts during verification.
